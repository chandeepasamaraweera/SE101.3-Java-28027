
package com.mycompany._lab1;

public interface myfirstinterface 
{ /*1.there is no difference eventhough we add public static final keyword or not because inside an interface all variables are considered as final*/
 int x=1;
 /*2.there is no difference eventhough we add abstract keyword or not because inside an interface all methods are considered as abstract*/
 void display();
         
                 
}
